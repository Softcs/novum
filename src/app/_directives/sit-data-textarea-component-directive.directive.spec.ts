import { SitDataTextareaComponentDirectiveDirective } from './sit-data-textarea-component-directive.directive';

describe('SitDataTextareaComponentDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SitDataTextareaComponentDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
