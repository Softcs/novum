import { SitTrackCapsDirectiveDirective } from './sit-track-caps-directive.directive';

describe('SitTrackCapsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SitTrackCapsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
